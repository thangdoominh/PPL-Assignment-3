string FourIV;
        int main(int global, int main) {
            putInt(exp);
            global = (getInt()*7);
            return global;
        }
        int exp;
        void factor(int a, int b) {
            a=b=exp;
            print(a+b);
            return ;
        }

        