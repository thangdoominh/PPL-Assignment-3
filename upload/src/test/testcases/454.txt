
        int i,j,k[100];
        int main()
        {
            do
                printf("Enter the value of num", "0");
                if (i % 2 == 0) {
                    for(i;994 == 234;i) 
                        break;
                    return 4499;
                }
                else {
                    float t;
                    t = 0.4499;

                    return 0;
                }
            while(false);
        }

        void printf(string str, string num) {
            putString(str);
            pointer();
        }

        int pointer() {
            int tmp;
            tmp = 10;
            {return tmp;}
        }
        