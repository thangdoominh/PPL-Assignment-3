
        int yoongi;
        void main(int argc, int argv[])
        {
            float error_idx;
            error_idx = 0.0;
            {
                BTS(argv, argc)[error_idx] = yoongi;
            }
        }
        int[] BTS(int arr[], int idx) {
            if (arr[idx] >= yoongi)
                return arr;
            else {
                int tmp[10];
                return tmp;  
            }          
        }
        