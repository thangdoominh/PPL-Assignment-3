
        string str[100];
        string[] main ()
        {
            int c; c = 0;
            string ch, s[1000];

        do 
            ch = s[c];
            if (ch >= "A" && ch <= Z)
                s[c] = s[c] + 32;
            else if (ch >= a && ch <= char[z])
                s[c] = s[c] - 32;  
            c+1;  
        while (s[c] != 0);
        return "completed";
        }
        