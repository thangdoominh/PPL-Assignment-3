
        int a[2];
        void main() {
            a[0] = a[1] = 3;
            float val;
            boolean b;
            b = (7.7 > 7) || (double(a)[1] - 1 >= 5);
            if (b) {
                val = (double(a)[1] * 7) % double(a)[0];
                putFloat(val);
            }
            else putBoolLn(b);
        }
        int[] double(int arr[]) {
            arr[0] = arr[0] * 2;
            arr[1] = arr[1] * 2;
            putFloatLn(arr[0] + arr[1]);
            return arr;
        }
        