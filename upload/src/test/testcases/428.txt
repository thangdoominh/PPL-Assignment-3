
	        float base;
	        void main(int argc, float argv[])
	        {
	            greater(argv, argc);
	            return ;
	        }
	        float[] greater(float arr[], int idx) {
	            if (arr[idx] >= base)
	                return arr;
	            else {
	                float tmp[10];
	                return tmp;  
	            }          
	        }
	        